specVersion: "v1beta"
serviceId: "appquestion"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "appquestion-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
